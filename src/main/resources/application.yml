server:
  port: 8088
  servlet:
    context-path: /api
  session:
    cookie:
      max-age: 2592000

spring:
  profiles:
    active: dev
  #session过期时间
  session:
    timeout: 86400
  main:
    allow-circular-references: true
  datasource:
      driver-class-name: ${bi.datasource.driver-class-name}
      url: jdbc:mysql://${bi.datasource.host}:${bi.datasource.port}/${bi.datasource.database}?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&useSSL=false&allowPublicKeyRetrieval=true
      username: ${bi.datasource.username}
      password: ${bi.datasource.password}
  redis:
    database: ${bi.redis.database}
    host: ${bi.redis.host}
    port: ${bi.redis.port}
    timeout: ${bi.redis.timeout}
    password: ${bi.redis.password}
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: false
  global-config:
    db-config:
      logic-delete-field: isDelete
      logic-delete-value: 1
      logic-not-delete-value: 0
#鱼聪明ai
yuapi:
  client:
    access-key: x51lglz7q6q1ge407lewueknd9eh6j2n
    secret-key: o4y0ttqbox0f33u005pgquijwv8t06ci
